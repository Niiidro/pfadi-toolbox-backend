### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Activity {
  chief: Leader!
  date: String!
  id: Int!
  items: [Item!]!
  location: Location!
  planned: Boolean!
  program: Program!
  step: Step!
  type: Type!
}

type Item {
  activity: Activity!
  duration: Int!
  id: Int!
  stuff: String
  task: String!
  time: String!
}

type Leader {
  activities: [Activity!]!
  id: Int!
  name: String!
}

type Location {
  activities: [Activity!]!
  city: String!
  id: Int!
  place: String
}

type Mutation {
  createActivity(chief: Int!, date: String!, location: Int!, planned: Boolean!, program: Int!, step: Int!, type: Int!): Activity!
  createItem(activity: Int!, duration: Int!, stuff: String, task: String!, time: String!): Item!
  createLeader(name: String!): Leader!
  createLocation(city: String!, place: String): Location!
  createProgram(split: Int!, title: String!, year: Int!): Program!
  createSettings(dark: Boolean, semester: Boolean, startTime: String): Settings!
  createStep(name: String!): Step!
  createType(name: String!): Type!
  deleteActivity(id: Int!): Activity
  deleteItem(id: Int!): Item
  deleteLeader(id: Int!): Leader
  deleteLocation(id: Int!): Location
  deleteProgram(id: Int!): Program
  deleteSettings(id: Int!): Settings
  deleteStep(id: Int!): Step
  deleteType(id: Int!): Type
  updateActivity(chief: Int, date: String, id: Int!, location: Int, planned: Boolean, program: Int, step: Int, type: Int): Activity
  updateItem(activity: Int, duration: Int, id: Int!, stuff: String, task: String, time: String): Item
  updateLeader(id: Int!, name: String): Leader
  updateLocation(city: String, id: Int!, place: String): Location
  updateProgram(id: Int!, split: Int, title: String, year: Int): Program
  updateSettings(dark: Boolean, id: Int!, semester: Boolean, startTime: String): Settings
  updateStep(id: Int!, name: String): Step
  updateType(id: Int!, name: String): Type
}

type Program {
  activities: [Activity!]!
  id: Int!
  split: Int!
  title: String!
  year: Int!
}

type Query {
  activities(program: Int): [Activity!]!
  activity(id: Int!): Activity!
  item(id: Int!): Item!
  items(activity: Int): [Item!]!
  leader(id: Int!): Leader!
  leaders: [Leader!]!
  location(id: Int!): Location!
  locations: [Location!]!
  program(id: Int!): Program!
  programs: [Program!]!
  settings(id: Int!): Settings!
  step(id: Int!): Step!
  steps: [Step!]!
  type(id: Int!): Type!
  types: [Type!]!
}

type Settings {
  dark: Boolean!
  id: Int!
  semester: Boolean!
  startTime: String!
}

type Step {
  activities: [Activity!]!
  id: Int!
  name: String!
}

type Type {
  activities: [Activity!]!
  id: Int!
  name: String!
}
